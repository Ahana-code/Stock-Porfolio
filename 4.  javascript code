##This JavaScript code updates the stock prices every 5 seconds without refreshing the page

setInterval(() => {
    fetch('/get-updated-stocks')
        .then(response => response.json())
        .then(data => {
            // Update table rows dynamically
        });
}, 5000);  // Update every 5 seconds


Runs every 5 seconds
Calls the backend (/get-updated-stocks)
Receives new stock prices
Updates the table dynamically









